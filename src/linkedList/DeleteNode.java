package linkedList;

/**
 * 给定一个节点 O(1)的时间复杂度下删除这个节点
 */
public class DeleteNode {

    // 将当前节点的值修改为后继节点的值 再删除后继节点
    // todo 当待删除节点本身是尾节点的时候  算法使用O(n)级别复杂度
    public static void deleteNode(ListNode head,ListNode toDelete){

    }
}
